# 待办清单 · 未完成功能 (Backlog)

> 第一期（登录 + 只读浏览 + 个人信息）已完成并接入真实 Discuz 移动 API。
> 本文件记录**已完成项**与**尚未完成的功能**，按优先级排列。
> 接口模块对照 [02-API.md](02-API.md)。

---

## ✅ 已完成（第一期）

- 账号密码登录（`login`）、登录态持久化（Cookie / `checkAuth` 冷启动恢复）、退出登录、游客浏览
- 论坛首页板块树（`forumindex`）、板块帖子列表 + 作品标签筛选 + 上拉加载更多 + 下拉刷新（`forumdisplay`）
- 帖子详情：正文 + 楼层回复、富文本（文本 / 图片 / 引用）解析、图片大图查看器（`viewthread`）
- 个人主页（自己 / 他人）（`profile`），我的收藏（`myfavthread`）
- 消息：提醒（`mynotelist`）、私信会话列表（`mypm`，只读）、底部未读角标（`notice`）
- 浏览历史（本地）、设置（深色模式 / 字号 / 清缓存占位）、关于
- 真实头像与帖内图片加载、加载 / 空 / 错误重试态、浅深色主题切换并记忆
- 运营方授权已取得；Web 端通过本地 CORS+Cookie 代理验证，Android 原生直连

---

## ⬜ P0 · 上线前仍需处理

| 项 | 说明 | 状态 |
|---|---|---|
| 登录验证码降级 | 多次失败触发 `seccode` 时降级到 WebView 登录并回收 Cookie | ⬜ 待开发 |
| 风控适配 | `acw_tc`/`cdn_sec_tc`(ESA) 风控、频率限制的退避与重试（目前仅基础错误提示） | ⬜ 待完善 |
| 富文本增强 | 楼层 `message` 的隐藏内容/折叠码/附件下载、相对路径补全的边界场景 | ⬜ 部分完成 |
| 字段容错层 | API 非官方契约，进一步做缺字段/类型容错与版本探测 | ⬜ 待加固 |
| 原生 Android 冷启动登录态 | 当前原生依赖系统 Cookie；冷启动恢复需接 `@react-native-cookies` 持久化 `auth`/`saltkey` | ⬜ 待开发 |

## ⬜ P1 · 写操作（v2 主线）

| 功能 | API 模块 | 备注 |
|---|---|---|
| 发新帖 / 回帖 / 编辑 | `newthread` / `sendreply` / `editpost` | 需 formhash、富文本/图片上传 |
| 图片 / 附件上传 | `forumupload` | 与发帖/回帖联动 |
| 收藏 / 取消收藏 | `favthread` | 详情页 ♡ 真正写入（目前仅前端态 + 提示） |
| 评分 / 点评 | `reppost` / `comment` | 文学区"加分代替灌水"社区习惯 |
| 发送私信 / 会话详情 | `sendpm` / `pmlist&touid=` | 目前仅只读会话列表 |
| 关注 / 好友 | `follow` / `friendlist` | 个人主页"关注/私信"按钮目前置灰 |
| 每日签到 | 插件 `plugin.php?id=zqlj_sign` | 非标准移动 API，可能需 WebView |
| 标记提醒/私信已读 | 写操作 | 消息页"全部已读"目前为占位 |

## ⬜ P2 · 浏览增强

| 功能 | 说明 |
|---|---|
| 原生全文搜索 | 移动 API `search` 不稳定；首页搜索目前为占位，需 WebView 降级或 `searchid` 二跳 |
| 帖子跳页 / 跳楼 / 只看楼主 | 大帖分页、定位楼层（详情目前仅加载第 1 页） |
| 帖子排序 / 筛选增强 | orderby（最新回复/发表/回复数/查看数）、digest/置顶过滤 |
| 收藏 / 历史分页 | 收藏与浏览历史目前仅首页 / 本地，未做翻页 |
| 我的发帖 / 回复 | `mythread&view=thread\|reply`（"我的发帖"目前为占位） |
| 离线缓存 / 常去板块 | 帖子正文/图片缓存、自定义常用板块 |
| 字号设置生效 | 设置页字号选择目前未联动正文渲染 |

## ⬜ P2 · 通知与推送

| 功能 | 说明 |
|---|---|
| 提醒深度跳转 | 点提醒跳到对应帖子/楼层（解析 `from_idtype`） |
| 系统推送 | APNs/FCM；服务端无推送，需自建轮询或反代 |

## ⬜ P3 · 工程化与体验

- 主题换肤、字号无障碍、简繁转换显示选项
- 多账号切换、崩溃/埋点监控、API 失败率告警
- 注册新账号 / 找回密码：跳官网 WebView

---

## 已知技术债 / 待验证

1. `mythread` / `myreply` 字段未在有发帖的账号上充分验证。
2. `profile.extcredits1..8` 各维度积分**业务名称**需对照站点积分设置确认。
3. 楼层"隐藏内容(需回复可见)""折叠码"的处理策略未定。
4. 移动 API 限流阈值未知，需压测确定安全频控。
5. 富文本解析（`src/util.js` `parseMessage`）目前覆盖文本/图片/引用，表格等复杂结构待补。
